<div class="marquee">
  <div class="marquee__track">
    {% for i in (1..8) %}
      <span>{{ settings.marquee_text }}</span>
    {% endfor %}
  </div>
</div>

{% schema %}
{
  "name": "Marquee",
  "settings": []
}
{% endschema %}
